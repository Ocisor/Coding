import random 

print(random.randint(1,6))

class Player:
    def __init__(self):
        self.nets = 1
        self.cash = 100

    buyNets 
    
    def riskyTurn():
        weather = random.randint(1,6)
        if weather == 5:
            weather = "rain"
        elif weather == 6:
            weather = "storm"
        else:
            weather = "clear"
        

        